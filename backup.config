# Backup destination directory
BACKUP_DEST="$HOME/backups"

# Exclude these folders/files (comma-separated)
EXCLUDE_PATTERNS=".git,node_modules,.cache,*.log"

# Retention policy
DAILY_KEEP=7
WEEKLY_KEEP=4
MONTHLY_KEEP=3

# Checksum type: sha256 or md5
CHECKSUM_TYPE="sha256"
